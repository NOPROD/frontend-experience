image: node:latest

stages:
  - prepare
  - build
  - deploy

dependencies:
  stage: prepare
  script:
    - apt-get update -qq && apt-get install -y -qq lftp
    - npm install

build:
  stage: build
  script:
    - npm build

send to ftp:
  stage: deploy
  script:
    - lftp -c "set ftp:ssl-allow no; open -u $FTP_USERNAME,$FTP_PASSWORD $HOST; mirror -Rev dist/ ./public_html  --ignore-time --parallel=10    --exclude-glob .git* --exclude .git/"
